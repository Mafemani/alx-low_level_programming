#include <stdio.h>
/**
 * main - Entry point of the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/**
	 * puts - print a string to the console followed by a newline character
	 *
	 * Description: This function takes a string as input and prints it to
	 * the console, followed by a newline character.
	 *
	 * Return: On success, returns a non-negative integer.
	 * On error, returns EOF.
	 */
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return 0;
}
